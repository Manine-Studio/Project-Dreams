using Progress_System;
using UnityEngine;

namespace HUDSystem.MapSystem
{
    public class MapConditionChecker : MonoBehaviour
    {
        [SerializeField] private Condition _PreConditions;

        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
